{
  "name": "shared",
  "version": "1.0.0",
  "description": "shared folder for golden calf project",
  "main": "script.js",
  "scripts": {
    "start": "npm run updateShared && npm run --prefix ../backend start & npm run --prefix ../frontend dev",
    "updateShared": "npx ts-node ../shared/script.ts"
  },
  "author": "Ty Fischer",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.10.3",
    "deso-protocol": "^2.6.4",
    "fs-extra": "^11.2.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4"
  }
}
